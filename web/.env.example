# AuditEng Web Dashboard Environment Variables
# Copy this file to .env.local and fill in the values

# API URL for production deployment (REQUIRED in production builds)
# - Development: Leave empty to use Vite proxy (localhost:8000)
# - Production: Set to your backend API URL
#
# IMPORTANT: This must be set at BUILD TIME, not runtime!
# Vite bakes this value into the JavaScript bundle during build.
#
# Railway deployment example:
#   VITE_API_URL=backend-api-production-c20f.up.railway.app
#
# If this URL is wrong or points to a non-existent service,
# API calls will fail with ERR_CONNECTION_TIMED_OUT.
VITE_API_URL=
